
import {dirname: getDirectory, join:joinPath} from "node:path";

const testDirectory = (path, options) => {

};

const spawn

const testFile = (path, options) => {
  // spawnSync();
  const dirname = getDirectory(path);
  appendFileSync(
    joinPath(getDirectory(path), options.memoization),

  );
};
